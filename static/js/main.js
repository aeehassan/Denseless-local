/**
 * DenseLess — Global JavaScript
 * ================================
 * Minimal JS entry point.
 * Feature-specific scripts go in {% block scripts %}.
 */

document.addEventListener("DOMContentLoaded", () => {
    console.log("DenseLess loaded.");
});
